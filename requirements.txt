# Slim runtime deps for the DEPLOYED Streamlit Cloud app (serve-only).
# Full data/training stack lives in requirements-full.txt.
# Trimmed to fit Streamlit Cloud's 1 GB RAM limit and keep builds fast.
streamlit==1.56.0
tornado==6.5.5
fastapi==0.135.3
uvicorn==0.42.0
pydantic>=2
numpy
pandas
pyarrow<25
xgboost>=2.0
shap
pyyaml
google-generativeai
groq
